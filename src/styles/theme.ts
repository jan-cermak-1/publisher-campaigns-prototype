import { colors, typography, spacing, borderRadius, shadows, transitions, breakpoints, zIndex } from './soulTokens';

export const theme = {
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
  transitions,
  breakpoints,
  zIndex,
};

export type Theme = typeof theme;

